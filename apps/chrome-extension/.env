# Backend base URL for WebSocket (protocol and host:port)
# Examples:
#   ws://localhost:3001
#   wss://api.suaempresa.com
EXT_BACKEND_WS_BASE=ws://localhost:3001

# Audio egress WebSocket path on backend
# Default configured in backend main.ts is /egress-audio
EXT_EGRESS_AUDIO_PATH=/egress-audio

# Optional defaults (can be overridden by popup or code)
EXT_DEFAULT_SAMPLE_RATE=16000
EXT_DEFAULT_CHANNELS=1
