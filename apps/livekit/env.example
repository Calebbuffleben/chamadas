# Copy to .env.livekit and customize for your environment

# LiveKit API credentials (must match livekit.yaml keys)
LIVEKIT_API_KEY=devkey
LIVEKIT_API_SECRET=devsecret

# Local service URL (used by clients to connect)
LIVEKIT_WS_URL=ws://localhost:7880

